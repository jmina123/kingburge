document.addEventListener('DOMContentLoaded',()=>{const items=document.querySelector('#cart-items'),view=document.querySelector('#cart-view'),confirm=document.querySelector('#confirmation');function render(){const cart=KB.cart;if(!cart.length){items.innerHTML='<div class="cart-empty"><h2>Tu carrito está vacío</h2><p>Hay algo increíble esperándote en el menú.</p><a class="button" href="index.html#menu">Ver menú</a></div>';document.querySelector('#checkout').disabled=true;}else{items.innerHTML=cart.map(p=>`<article class="cart-item"><img src="${p.image}" alt="${p.name}"><div><span class="tag">${p.category}</span><h3>${p.name}</h3><p>${KB.money(p.price)} c/u</p></div><div class="cart-controls"><div class="quantity"><button data-action="minus" data-id="${p.id}">−</button><output>${p.qty}</output><button data-action="plus" data-id="${p.id}">+</button></div><b>${KB.money(p.price*p.qty)}</b><button class="remove" data-action="remove" data-id="${p.id}">Eliminar</button></div></article>`).join('');document.querySelector('#checkout').disabled=false}const sub=cart.reduce((a,p)=>a+p.price*p.qty,0),tax=sub*.1;document.querySelector('#subtotal').textContent=KB.money(sub);document.querySelector('#tax').textContent=KB.money(tax);document.querySelector('#total').textContent=KB.money(sub+tax);updateHeader()}items.onclick=e=>{const b=e.target.closest('[data-action]');if(!b)return;let cart=KB.cart,p=cart.find(x=>x.id===b.dataset.id);if(b.dataset.action==='plus')p.qty++;if(b.dataset.action==='minus')p.qty--;if(b.dataset.action==='remove'||p.qty<1)cart=cart.filter(x=>x.id!==b.dataset.id);KB.cart=cart;render()};document.querySelector('#checkout').onclick=()=>{if(!KB.user){location.href='login.html';return}KB.cart=[];view.hidden=true;confirm.hidden=false;document.querySelector('#order-number').textContent=`Pedido #KB-${Math.floor(10000+Math.random()*89999)}`;updateHeader()};render()});
